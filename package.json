{
	"name": "simple-frp",
	"version": "0.0.1",
	"author": "CoderPuppy",
	"license": "MIT",

	"dependencies": {
		"es6-set": "^0.1.0",
		"right-now": "^1.0.0"
	},

	"scripts": {
		"test": "browserify -d -t coverify `find tests -type f -name '*.js'` | node | coverify | faucet"
	},

	"devDependencies": {
		"tape": "^2.13.3",
		"browserify": "^4.2.0",
		"coverify": "^1.0.7",
		"faucet": "^0.0.1"
	}
}